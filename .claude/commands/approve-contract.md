---
description: Record the human decision on an intent and its canonical contract
argument-hint: <intent-id> <alternative> [notes]
---

<!-- Generated by pactwright from @pactwright/standard@0.0.1 (pactwright runtime). Do not edit; run `pactwright sync`, which overwrites this file and removes it once it is no longer rendered. Delete this line to take ownership. -->

# /approve-contract <intent-id> <alternative> [notes]

## 1. Ask the runtime first

Run `pnpm pactwright context <intent-id>` for the current lineage and `pnpm pactwright lifecycle status --intent <intent-id>` for the completed stages. The runtime owns the lifecycle: it decides what is permitted, who may decide and how records are linked. Never reason about that yourself.
If the runtime reports a validation problem, or lists `approve-contract` as already completed, show its output and stop. The runtime refuses any record that is out of order.

## 2. Synthesise the canonical contract

`$ARGUMENTS` is the intent id, the label of the chosen alternative (or
`reject` / `defer`) and optional notes. This command is run by a human;
their choice is the decision.

Use the `spec` agent (from `.claude/agents/spec.md`) for this. Give it the runtime context verbatim. For a chosen alternative, write the one canonical contract from it (the contract-writing skill applies). For reject or defer, write nothing but the decision body.

## 3. Hand the result to the runtime

Write a YAML file in a temporary location outside the repository with:

```yaml
intent: <intent-id>
outcome: proceed | reject | defer
decided_by: human:<handle>  # short handle for the deciding human, no spaces (e.g. human:samir)
body: |
  <why this alternative; include the notes>
contract:                   # proceed only
  title: <contract title>
  body: |
    <canonical contract>
```

Then run `pnpm pactwright lifecycle record approve-contract --file <path>`.
The runtime checks the transition, validates the complete graph and writes
the record. Do not create or edit anything under `specs/` yourself.
Alternatives that were not chosen are not saved anywhere.

## 4. Stop

Show the runtime output verbatim and stop. Do not run another adapter
command; the user decides what to do next.
