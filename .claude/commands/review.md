---
description: Review delivered changes against the contract and brief
argument-hint: <brief-id>
---

<!-- Generated by pactwright from @pactwright/standard@0.0.1 (pactwright runtime). Do not edit; run `pactwright sync`, which overwrites this file and removes it once it is no longer rendered. Delete this line to take ownership. -->

# /review <brief-id>

## 1. Ask the runtime first

Run `pnpm pactwright context <brief-id>` for the current lineage and `pnpm pactwright lifecycle status --intent <intent-id>` for the completed stages. The runtime owns the lifecycle: it decides what is permitted, who may decide and how records are linked. Never reason about that yourself.
If the runtime reports a validation problem, or lists `review` as already completed, show its output and stop. The runtime refuses any record that is out of order.

## 2. Review

Use the `reviewer` agent (from `.claude/agents/reviewer.md`) for this. Give it the runtime context verbatim. Review the contract, the brief, the delivered changes and the required verification. Report findings with file references.

## 3. Report, do not record

This stage leaves no graph record. Present the result to the user. Do not
create or edit anything under `specs/`.
Review reasoning is not graph state.

## 4. Stop

Show the runtime output verbatim and stop. Do not run another adapter
command; the user decides what to do next.
