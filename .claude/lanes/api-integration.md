---
eligible_agents: [api-implementer, backend-implementer]
default_agent: api-implementer
---

## Owns

API surfaces and third-party integration

## Dependency hints

- `domain-backend` — the behaviour the API surface exposes
- `data-migration` — the schema the payloads and contracts are shaped by
