---
eligible_agents: [ui-implementer, api-implementer]
default_agent: ui-implementer
---

## Owns

UI and client code

## Dependency hints

- `product-spec` — the interaction spec and acceptance the UI is built to
- `domain-backend` — the behaviour and data the client renders
