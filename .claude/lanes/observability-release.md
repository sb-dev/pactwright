---
eligible_agents: [ops-implementer, backend-implementer]
default_agent: ops-implementer
---

## Owns

telemetry, runtime, release

## Dependency hints

- `domain-backend` — the runtime being instrumented and released
- `api-integration` — the surfaces whose availability and errors are measured
