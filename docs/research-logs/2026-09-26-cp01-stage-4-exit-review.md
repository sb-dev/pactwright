# Checkpoint 1 Stage 4 — T2 stage-level exit review

**Scope:** Spec 00 T2 for Checkpoint 1 Stage 4 (CP01-S14 to CP01-S19) under the [resolution methodology](../open-question-resolution-methodology.md) v2 §7. Reviewed on branch `claude/stage-4-step-contracts`, base `refactor/pactwright-v2` at `0ef869c`. Source revisions at review: Core v6, Distribution v5, Implementation Guide v17, Principles v4, Checkpoint 1 v29, Spec 00 v5, contract format 2. Questions Q41–Q51 keep their crosswalk IDs and wording; the batch records [B15](./2026-09-26-cp01-stage-4-b15-init-and-scaffold.md), [B16](./2026-09-26-cp01-stage-4-b16-extension-composition.md), [B17](./2026-09-26-cp01-stage-4-b17-environment-identity.md) and [B18](./2026-09-26-cp01-stage-4-b18-doctor-and-runtime-upgrade.md) remain the decision records.

## Method

Two independent read-only reviewers ran on the uncommitted tree. One checked whole-stage consistency: the Q41–Q51 dispositions under the two-implementations test; identity, storage, authority and failure agreement across S14–S19 and with Distribution v5 / Core v6; requirement coverage, case distinctness, binding uniqueness, `requires` and `inputs` order, deliverable-summary equality and loss against `0ef869c`; pinned behaviour against the released `207ac08` code; and the env1 vectors. The other audited cross-stage allocation: contradictions with requirement-ready Stages 1–3 and their exit follow-ups, the Stage 5 contracts, the unconverted Steps 22–31, Checkpoint 2 §3 and Steps 7 and one-shot `init --github`, Checkpoint 5 and Distribution §§6–8, the Guide and Principles, the exit gate and every later owner the records name. Each refuted its own candidates before reporting. The author verified every finding against the text before acting.

## Findings and dispositions

The two reviews ran at `c750a60`. Blocking findings, applied in this pass:

| ID | Location | Finding | Correction |
|---|---|---|---|
| X1 | Distribution §15 migration table, "Released unversioned lock"; S19/AC14 | The row still required a "compatible complete target environment", so for the real consumer path (`@pactwright/standard@0.0.1` pins `pactwright: 0.0.1`) one implementation refuses the released-format migration and another completes it runtime-ahead, and S19/AC06–AC07 could not tell them apart. | The row records a selected pack whose declared range excludes the target unchanged under the runtime-ahead rule. S19/AC14 `released-migration-with-pinned-pack`: the AC06 project with the pinned pack migrates, writes the version 1 lock with the pack unchanged, exits zero and reports the pack as action required. |
| X2 | Distribution §13; S15/R03, AC09 | "An incompatible resolution is rejected before a new environment is accepted" and "the lock is written only after agreement and validation succeed" contradicted the runtime-ahead lock, so an implementation passing S19/AC14 failed S15/AC09. | §13, S15/R03 and AC09 carve out the runtime-ahead lock as the one exception: it records an incompatible component unchanged and is not an accepted executable environment. |

Non-blocking findings, applied:

| ID | Correction |
|---|---|
| X3 | Distribution §15 and S19/R05 pin what `upgrade` does while a recovery record exists: only `--to` naming the record's recovery target (the previous runtime after an interrupted or failed upgrade, the current runtime after a failed rollback) is accepted; it writes no new record, performs the recovery the record's stage requires (byte restoration and the seam's restore request, or re-running the forward migration over released-format stores) and removes the record; any other invocation is refused naming the record. S19/AC05 `other-target-refused`; the `interrupted-after-replacement` case asserts the remediation and that no new record is written. Doctor's remediation names the record's recovery target (§16, S18/R05). |
| X4 | Distribution §3 states that `validate` reports the scaffold valid and exits zero, the missing selection being doctor's finding; S14/R03 and AC02 `validate-passes`. |
| X5 | The seam's removal request covers an Extension's package on `extension remove` (Distribution §15, S11/R08), instead of a separate sentence beside the request list. |
| X6 | S14/AC02 no longer derives `environment_lock_hash` at Step 14, which S11/R09 reserves for Step 15; R03 defers to CP01-S15/AC11 (second reviewer). |
| X7 | S18/AC02 no longer builds a Step 19 recovery record; the interrupted-upgrade diagnosis stays with S19/AC05, where the record exists (second reviewer). |
| X8 | S17/AC03 `incompatible-pack` proves sync refuses the runtime-ahead lock that Distribution §15 relies on (second reviewer). |
| X9 | S14/R01 qualifies "creates no generated directory" to plain init (second reviewer). |
| X10 | S18 declares the `package-manager-delegation` input its version listing uses (second reviewer). |
| X11 | Distribution §16 and S18/R04, R05, AC02 `disabled-in-configuration-only`: a configuration ↔ lock disagreement about an Extension's enabled state, such as a hand-edited `enabled: false` whose lock entry remains, is action required with `pactwright extension disable <id>` or `extension add <id>` as the remediation, so Principles §11's "edit the owning configuration" path has a defined outcome (second reviewer). |
| X12 | Distribution §15 rollback blockers include a project with no selected pack, since the released lock needs one (second reviewer). |
| X13 | Distribution §3's refusal clause covers `--github` without a selection, which Checkpoint 2 line 451 expects (second reviewer). |
| X14 | S18/AC02 `lock-drift` names the drifted component (the fixture Extension), so its remediation is testable. |
| X15 | Crosswalk header names the `affects` entries this pass added (S15/AC11, S16/AC06); Q47 gains S17/AC03. |
| X16 | B18 names Checkpoint 2 Step 17, not Step 7, and records the removal of S19/AC05 `no-compatible-environment` and R05's draft clause, split into R12/AC14 and AC05 `missing-capability`. |
| X17 | The env1 fixture's `fixture-base` entry carries a `graph` block, as Distribution §12 requires of an enabled Extension entry; its digests were regenerated and the definition test passes. |
| X18 | This record exists; the first commit `c750a60` carried it as pending. |

**Fresh review of X1–X17 (methodology §6):** a fresh read-only reviewer examined the corrections at `747e28a`, confirmed all seventeen present and consistent, re-ran the contract, format and test checks and the env1 recomputation, and returned ACCEPT with three non-blocking wording items and four nits, applied here: X19 (§16, S18/R05: while a recovery record exists the interrupted-upgrade remediation is the only upgrade command named), X20 (§16, S18/R05: `extension disable` when configuration disables an Extension whose lock entry remains, `extension add` when configuration enables one whose lock entry is absent), X21 (S19/R10 lists no selected pack among the rollback blockers), X22 (Checkpoint 2 line numbers 450 and 666 below), X23 (S15/R03 wording).

**Owner review [5326242471](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326242471) at `c750a60`** returned CHANGES REQUIRED with four findings, applied as follows:

| ID | Owner finding | Correction |
|---|---|---|
| X24 | P1, combined incompatible components: after `upgrade --to`, upgrading either a pinned pack or a pinned Extension first would fail on the other through sync, so both repair orders rolled back, and an unchanged exact pin cannot be escaped by the owning command alone. | Distribution §15: while the environment is runtime-ahead, `agent-pack use`, `agent-pack upgrade` and `extension upgrade` validate the candidate against the other components as recorded, lock it, defer `sync` until no component remains runtime-incompatible and report the pending ones; the last repair runs `sync`; ordinary execution stays refused; the remediation names the constraint change before the owning command. §16, S11/R04, S16/R06, S18/R05, AC02; S19/R12, AC14 `unchanged-pin-no-op`, `pack-then-extension`, `extension-then-pack`, `dependent-extensions-pinned`. |
| X25 | P1, recovery after a completed migration: the record held no canonical bytes, so a `sync` or `validate` failure or interruption after a format-changing migration committed left the old runtime facing new-format stores; §15's retained "preserve graph bytes" instruction conflicted with the no-copy reasoning. | Distribution §15: before a migration runs the record holds the previous bytes of every canonical file its plan changes or removes and the list of files it creates; failure after replacement restores every recorded byte, removes created files, requests the previous runtime, verifies the restored environment loads under its recorded format and removes the record; the migration-boundary paragraph now says the same. S19/R05, AC05 `migration-then-sync-failure`, `migration-then-validation-failure`, `interrupted-after-migration`; AC12. |
| X26 | P2, the released `0.0.1` CLI has no `upgrade` command, so it can neither write the record nor hand off, and after rollback it cannot run the record protocol; acceptance must use the unchanged released artefact. | Distribution §15: the supported entry from a pre-protocol runtime is package replacement followed by `upgrade --to <installed version>`, which performs the post-replacement steps itself with no package request when the installed runtime already is the target; rollback to `0.0.1` is completed entirely by the current runtime, which reverse-migrates, writes the released lock, validates with the released decoders, removes the record and requests replacement last, after which the released runtime's own `validate` must pass; acceptance runs against the published `0.0.1` artefact acquired through the seam. §16 and S18/R05, AC02 `runtime-behind-installed`; S19/R01, R06, R10, AC06, AC07, AC10, AC12. |
| X27 | P2, S14/AC02 derived the Step 15 hash at Step 14. | Already applied as X6 in `747e28a`. |

The owner's follow-up review [5326269695](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326269695) at `747e28a`, submitted before `18fd0ad` carried X24–X27, confirmed X6 and restated findings 1–3 with two additional details, applied as X28: an interruption cut point after the new lock is written and before sync (Distribution §15 failure paragraph; S19/AC05 `interrupted-after-lock-write`), and acceptance of the released-format migration beginning by invoking the unchanged published `0.0.1` CLI (S19/AC07).

Later-stage follow-ups, owned and non-blocking for Stage 4:

- **Checkpoint 2 (owner decision; not edited by this pass):** §3 line 104 "Every completed operation must leave a compatible resolved environment", line 106 "regenerates locks and runs validation", line 110 "verify … compatibility of all still-enabled components" and Step 17 lines 669 and 700 ("the still-compatible selected pack"; "Record compatibility … after each upgrade") are inconsistent with the runtime-ahead state, in which `pactwright upgrade --to 0.0.2` on a `0.0.1` project exits zero with the pack reported as action required and runs no sync; Step 17's `pnpm pactwright upgrade --to 0.0.2` (line 665) must now be preceded by `pnpm add -D pactwright@0.0.2`, since the released runtime has no `upgrade` command, which contradicts line 661 "Do not preinstall `0.0.2` with `pnpm add`" and §3 line 106 "do not preinstall the package"; line 449 should read `init --agent-pack <source>` rather than a "selection interaction", and line 450's refusal is now Distribution §3's. Line 666's `validate` after the runtime upgrade can pass, since Core §57 has no environment-compatibility rule; the owner should confirm that reading.
- **Checkpoint 2 (`init --github`):** init on a partial scaffold (configuration present, lock absent) and `init --with`/`--github` on an initialised project with a pack already selected and no `--agent-pack` are not pinned by Stage 4; Distribution §3 covers the initialised-project case only for a named selection.
- **Exit gate wording (optional):** the `pactwright upgrade` gate line could name the runtime-ahead state, which Checkpoint 2 Step 17 now depends on.

## Stage-level result (§7)

Checks confirmed by the reviewers at `c750a60` and unchanged by the corrections: every S14–S19 requirement has an exercising criterion; verifier bindings across CP01 are unique; case names are unique per criterion; `requires` and `inputs` are feasible in step order (Step 12 precedes Step 16; Step 11's seam output precedes Steps 16, 18 and 19); the Stage 4 deliverable summaries equal the contract `outputs` (46 of 46 across CP01); no requirement, criterion, `covers`, `source` or `verify` entry from `0ef869c` was removed without explanation (X16); Q41–Q51 each have a supported disposition; the Stage 1 exit review's Q42 constraint is met by the first branch (plain init writes a lock; Core §§54/56, S01/AC09 and S05/AC12 unchanged); the S09/R06 and S11/R08 amendments are the consequences B11 and B12 planned; Checkpoint 5 §4 and Distribution §§6–8 agree with "declared by the selected pack's installed manifest"; the Guide and Principles §§12, 20, 21 are not contradicted; every Stage 4 exit-gate line has an owning criterion; every later owner the records name exists. The claims the records make about the released `207ac08` code were verified against it, and the four env1 digests were recomputed independently.

With X1–X28 applied, the corrected tree is consistent with Distribution v5 and Core v6, and no unresolved behaviour needed by Stage 4 steps remains. X19–X28 were authored by the record's author after the accepting review of X1–X17 and so require fresh independent review (methodology §6); the verdict below records that review's result. This is scoped T2 work; it authorises no implementation, harness construction or acceptance.

**Fresh review of X19–X28 (methodology §6):** a fresh read-only reviewer examined the tree at `18fd0ad` plus X28, verified X21, X22 and X24–X27 present and consistent, confirmed both repair orders and the dependent chain feasible under S11/R04, S16/R06 and S17/AC03, recovery coherent after a completed migration, the released entry and rollback leaving nothing for the `0.0.1` binary to do, the mechanical checks and the Checkpoint 2 line numbers, and returned ACCEPT with nine non-blocking items. It found that X19, X20 and X23 were recorded but absent from the tree, because the script carrying them was chained after a failed step; they are applied now, with the other items, as X29–X36: X29 (§16, S18/R05: X19 and X20 applied, and a migration-required source names `upgrade --to <installed version>` when the installed runtime already is the target), X30 (S15/R03: X23 applied, and the runtime-ahead lock is refused by lifecycle run and capability invocation naming the component), X31 (B18 Q49 row no longer says pre-migration copies are never retained), X32 (§15, S19/R01, AC07 `completion-failure`: the completion path's record takes the lock's `runtime.version` as source and a failure on it restores the recorded bytes without a package change), X33 (S19/AC14 `dependent-extensions-pinned` declares a dependency range admitting both versions), X34 (S19/AC05's migration-then cases use a packed runtime that declares a core-store migration), X35 (§15, S19/AC10 `rollback-validation-fails`: a released-decoder validation failure after the reverse migration restores from the record), X36 (the Checkpoint 2 follow-up lists lines 661 and 106's "do not preinstall").

The owner's third review [5326299828](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326299828) at `a156de8` confirmed the Step 14 and multi-component items resolved and returned three findings, applied as X37–X39: X37 (Distribution §15, S19/R01, AC07 `completion-migration-failure`, `completion-validation-failure`, `completion-interrupted`, AC12: the completion path from the released runtime records the post-replacement package files as its baseline, since the source package state was replaced outside Pactwright before any record could capture it, and its recovery promise is stated narrowly, everything but the two package files restored); X38 (Distribution §15, S19/R05, R08, AC05, AC07, AC08: the record holds the exact previous path inventory of every core store and owner root the plan touches, and recovery restores inventory as well as bytes, so no created directory or entry survives, matching Core §55's bytes-and-inventory base); X39 (B18's challenge plan now records that check (4) first concluded a store copy unnecessary and was corrected, adds the bootstrap-baseline and residual-directory checks (6) and (7), and lists X28's cut point; this record's fresh-review range reads X19–X28 throughout).

**Fresh review of X37–X39 (methodology §6):** a fresh read-only reviewer examined the tree at `16e7b5b`, confirmed X38 and X39 satisfied, and returned CHANGES REQUIRED on one clause of X37: the record-driven `upgrade --to <previous>` on a record the completion path wrote still pointed at the general restoration, which requests the previous runtime through the seam, so one implementation would reinstall `0.0.1` and another would not. The owner's fourth review [5326329519](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326329519) at `9a88e61`, submitted before `16e7b5b`, found the same gap with the required cut points, confirmed the caught-failure baseline resolved, and asked that this record's verdict reflect the review until the corrections receive fresh independent review. Applied as X40 (Distribution §15, S19/R01, R05, AC07, AC12, S18/R05, §16: a record identifies its path and designates its recovery target, the previous runtime for a record written before replacement and the installed runtime for a completion-path record; on that path `upgrade --to <installed version>` after an interruption at record creation, after the completed migration or after the lock write restores every `.pactwright/` file, core store and owner root to source bytes and inventory, requests no package change, leaves the package files at their recorded post-replacement bytes and removes the record; `--to 0.0.1` is refused; AC07 gains `completion-interrupted-after-record`, `completion-interrupted-after-migration`, `completion-interrupted-after-lock-write` and `completion-other-target-refused`, replacing `completion-interrupted`, and its failure cases name the injected step) and X41 (the reviewer's non-blocking items: R05 cross-references the R01 exception; the comparison scope is `.pactwright/` files, core stores and owner roots, not the installed dependencies; the inventory records each root's existence and covers roots the plan creates; AC05's resume clause restores inventory; B18's Q48 row aligned and check (8) added; its checks keep their historical numbering, (1)–(4), (6)–(8), (5)).

The owner's fifth review [5326340880](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326340880) at `16e7b5b`, submitted before `043a074`, found the same completion-path conflict X40 resolves and asked for the S19 output promise, B18's Q48 answer and this record's attributions to match the selected design; applied as X42 (S19 output `runtime-upgrade` and its Checkpoint 1 summary state that the entry from a runtime without this protocol recovers only the previous stores under the installed runtime; B18's Q48 failure sentence is scoped to records written before replacement and names each record's designated target). Attribution: X37–X39 answer review 5326299828 at `a156de8`; X40 answers review 5326329519 at `9a88e61` and the fresh review of X37–X39; X42 answers review 5326340880 at `16e7b5b`.

The owner's sixth review [5326354173](https://github.com/sb-dev/pactwright/pull/51#pullrequestreview-5326354173) at `0f85132` confirmed the designated target, cut points, inventory, output summary and attributions resolved and found one remaining ambiguity: the failed-restoration sentence still named `--to <previous version>` generically, which a completion-path record refuses. Applied as X43 (Distribution §§15, 16, S18/R05, S19/R05 and B18's Q47–Q48 rows name the record's designated recovery target everywhere; S19/AC07 `completion-restoration-failure` proves the record remains, doctor names `--to <installed version>`, `--to 0.0.1` is refused, and the retry requests no package change, restores exact bytes and inventory and removes the record).

**Fresh review of X29–X42 (methodology §6):** a fresh read-only reviewer examined the tree at `0f85132`, verified X29–X42 present and consistent, re-ran the contract, format and test checks, the coverage, binding-uniqueness and summary-equality scripts and the per-case distinguishability of every changed criterion, checked the five decision areas under the two-implementations test, and returned ACCEPT with three non-blocking items and five nits. Its third item is the owner's sixth-review finding, X43. The others are applied as X44: S19/R05's doubled apostrophes in a plain scalar corrected; S19/AC07 `migrates` and AC14 `released-migration-with-pinned-pack` assert that no record remains; the migration-boundary paragraph names removed files and path inventory; AC07's wording separates the failure and interrupted cases; AC05's interrupted resume names the seam restore request; S19/R10 carries the reverse-migration validation-failure outcome; B18's applied-changes and Q49 case lists match the current criteria.

**Verdict: pending.** X43 and X44 were authored by the record's author after the accepting review of X29–X42 and await fresh independent review under methodology §6; until that review accepts them this record does not state Stage 4 requirement-ready.

## Verification

Commands on the corrected tree, Node `v22.22.2`, pnpm `11.7.0`:

| Command | Result |
|---|---|
| `pnpm contracts:check` | PASS |
| `pnpm format:check` | PASS |
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS, 25/25 (20 existing, 5 env1 vector checks) |
| `pnpm build` / `pnpm verify` | FAIL, inherited: no runtime sources and no `@pactwright/standard` project; identical on `0ef869c` |

Recorded challenge-plan checks executed by the author: the released `207ac08` reads cited in B15–B18; the Checkpoint 2, 5 and later-checkpoint greps; deliverable-summary equality (46 of 46 outputs across CP01-S01–S21 equal their summaries); verifier-binding uniqueness across CP01; case-name uniqueness per criterion; independent recomputation of the four env1 digests in Python and in the Node definition test. Schema, citation and mapping checks establish structural consistency only. No verifier binding, runtime or harness was executed.
