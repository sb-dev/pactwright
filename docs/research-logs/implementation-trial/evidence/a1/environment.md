# A1 — environment and skills actually used

Recorded on 21 September 2026. Every value below has its source named, because
the runbook asks for the *resolved* configuration rather than an inferred one.

## Agent

| | Value | How it was obtained |
|---|---|---|
| Model | Opus 5 (1M context) | Reported by the running Claude Code session as its own model |
| Model id | `claude-opus-5[1m]` | Same; this is the exact resolved id, not a family name |
| CLI | Claude Code `2.1.220` | `claude --version` → `2.1.220 (Claude Code)` |
| Session kind | Interactive Claude Code in the maintainer's working copy | — |
| Reasoning effort | Raised for this session by the maintainer before A1 began | — |

No provider API credential was used for A1 and no headless `claude` invocation
was made. The one reference test that needs provider authentication stayed
opt-out; see [`baseline-checks.md`](baseline-checks.md).

## Toolchain

| Tool | Version | Command |
|---|---|---|
| Node | `v22.16.0` | `node --version` |
| pnpm | `11.7.0` (launcher) | `pnpm --version` |
| npm | `11.5.2` | `npm --version` |
| Corepack | `0.32.0` | `corepack --version` |
| git | `2.50.1 (Apple Git-155)` | `git --version` |
| GitHub CLI | `2.88.1 (2026-03-12)`, authenticated as `sb-dev` | `gh --version`, `gh auth status` |
| OS | macOS 15.7.3 (build 24G419), Darwin 24.6.0, x86_64 | `sw_vers`, `uname -mrs` |

Two package-manager facts matter later and are easy to misread:

- The reference declares `packageManager: pnpm@11.7.0`, so installing and
  verifying it used **pnpm 11.7.0**.
- The clean consumer fixtures declare no `packageManager`, so the same launcher
  resolved **pnpm 10.32.0** there. `doctor` reports that missing declaration as
  a warning, which is the fixture's doing and not a defect in the runtime.

The reference's declared Node support is `>=22 <23 || >=24 <25`. A1 exercised
Node 22 only. Node 24 was not run locally; GitHub Actions ran both on this SHA
(inspected, not executed by A1 — see [`baseline-checks.md`](baseline-checks.md)).

## Skills

`skills-lock.json` is identical at `main` and at the pinned reference — blob
`dd9a33d014c1f7c5b97c4664fbbf89402ec26ed2`, version 1, **29 pinned skills**.
Nothing was installed, upgraded, unpinned or edited during A1.

The runbook names two skills for A1. Both were loaded from the repository's own
pinned copies under `.claude/skills/`:

| Skill | Pinned source and content hash | Applied as |
|---|---|---|
| `verification-before-completion` | `obra/superpowers`, `skills/verification-before-completion/SKILL.md`, `sha256:b529822bdac4b197b180fe12225cff86b930b65910435103c10622df7f041651` | In full. Every status in this A1 record names the command that produced it, its exit code and its evidence file. Nothing is reported as passing that was not run in this session. |
| `acquire-codebase-knowledge` | `github/awesome-copilot`, `skills/acquire-codebase-knowledge/SKILL.md`, `sha256:06b7a3d0bb44b70b470247aacc2ffffe37c8ce64cc3f9178be6f392c81cb6d22` | **Relevant subset only**, as §1.3 directs. Applied: its evidence rule (every claim traceable to a file or to terminal output), its monorepo and generated-output cautions, and its separation of `dependencies` from `devDependencies`. Deliberately **not** applied: its output contract, which would write seven documents into `docs/codebase/`. A1 may commit only planning and evidence files, and a parallel architecture-documentation set is not an A1 deliverable. What that skill would have discovered is recorded instead in [`authorities-and-public-surface.md`](authorities-and-public-surface.md). |

Its bundled `scripts/scan.py` was not run: its output is a repository inventory,
which A1 produced directly from `git ls-tree` and the measurement probe, both of
which are reproducible from this record.

No other skill was loaded during A1.

## Repository instructions read

- `CLAUDE.md` — commit format (`<type>: <imperative summary>`), exactly one
  co-author trailer naming the model, no session URLs or tool advertisements,
  and `pnpm verify` before committing. A1's commits follow it.
- `CONTRIBUTING.md` — `pnpm verify` is the one gate; `packages/standard/pack.yml`
  carries its own version and the exact compatible `pactwright` version;
  lifecycle and graph semantics belong to the runtime, never to prompts.
- `README.md` — the documented installation and command surface A1 exercised.
- `.claude/` — three agent definitions and seven command definitions, all
  generated by `pactwright sync` from the locked pack.

## Honest limits of this record

- The model id is what the running session reports about itself. It is not
  independently verifiable from inside the session, and A1 does not claim to
  have verified it against a provider response.
- Versions are those of the A1 machine on 21 September 2026. Any later session
  must re-record rather than inherit them.
