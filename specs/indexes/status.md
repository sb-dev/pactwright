# Status

Open work per live intent, with the next step derived from the graph.
`wave: —` means no wave was persisted; `issue: not synced` means no issue is recorded.
Generated by `spec:index` from the graph — never hand-edited.

## intent-docs-arrow-lint-e7b3 — Lint arrow semantics in repository markdown

stage: `intent-open`

next:

- `/propose-contracts intent-docs-arrow-lint-e7b3` — _paste_ — intent is open with no live `proposes` candidate

## intent-implement-brief-graph-lane-b3f5 — Define how a lane whose deliverable is graph data is implemented within the lifecycle

stage: `intent-open`

next:

- `/propose-contracts intent-implement-brief-graph-lane-b3f5` — _paste_ — intent is open with no live `proposes` candidate

## intent-malformed-cutoff-finding-b3d7 — Red the graph on a malformed dated cutoff instead of silently disabling the gate

stage: `intent-open`

next:

- `/propose-contracts intent-malformed-cutoff-finding-b3d7` — _paste_ — intent is open with no live `proposes` candidate

## intent-self-guiding-delivery-loop-6d79 — Make the delivery loop self-guiding, lane execution visible, and the graph readable

stage: `intent-selected`

next:

- `/implement-brief brief-conveyor-commands-c14d` — _paste_ — brief is `draft` and carries no open patch market
- consider whether this lane deserves a patch market — the brief carries no `## Strategy tension` section — _action_ — class 3 permits a patch market (available per lane); no marker is recorded, and the resolver never infers tension
- `/prepare-evidence brief-conveyor-docs-9e31` — _paste_ — brief is `implemented`; evidence is the next lifecycle record
- `/implement-brief brief-conveyor-lane-catalog-2d5b` — _paste_ — brief is `draft` and carries no open patch market
- consider whether this lane deserves a patch market — the brief carries no `## Strategy tension` section — _action_ — class 3 permits a patch market (available per lane); no marker is recorded, and the resolver never infers tension
- `/implement-brief brief-conveyor-resolver-3f7a` — _paste_ — brief is `draft` and carries no open patch market
- consider whether this lane deserves a patch market — the brief carries no `## Strategy tension` section — _action_ — class 3 permits a patch market (available per lane); no marker is recorded, and the resolver never infers tension
- `/write-tests brief-conveyor-tests-4c86` — _paste_ — brief carries `lane: test-verification`; its tests are written by test-writer
- consider whether this lane deserves a patch market — the brief carries no `## Strategy tension` section — _action_ — class 3 permits a patch market (available per lane); no marker is recorded, and the resolver never infers tension

| brief | lane | owner | wave | issue | status | next |
|---|---|---|---|---|---|---|
| `brief-conveyor-ci-6a9f` | observability-release | — | — | not synced | implemented | `/implement-brief brief-conveyor-commands-c14d` |
| `brief-conveyor-commands-c14d` | api-integration | — | — | not synced | draft | `/implement-brief brief-conveyor-commands-c14d` |
| `brief-conveyor-docs-9e31` | docs-spec | — | — | not synced | implemented | `/prepare-evidence brief-conveyor-docs-9e31` |
| `brief-conveyor-lane-catalog-2d5b` | product-spec | — | — | not synced | draft | `/implement-brief brief-conveyor-lane-catalog-2d5b` |
| `brief-conveyor-resolver-3f7a` | domain-backend | — | — | not synced | draft | `/implement-brief brief-conveyor-resolver-3f7a` |
| `brief-conveyor-schema-graph-8b2e` | data-migration | — | — | not synced | implemented | `/implement-brief brief-conveyor-commands-c14d` |
| `brief-conveyor-tests-4c86` | test-verification | — | — | not synced | draft | `/write-tests brief-conveyor-tests-4c86` |

## intent-unbacked-addressed-guard-8c4e — Flag intents marked `addressed` without edge-backed provenance

stage: `intent-open`

next:

- `/propose-contracts intent-unbacked-addressed-guard-8c4e` — _paste_ — intent is open with no live `proposes` candidate

## intent-write-tests-unlaned-brief-b3d8 — Define `/write-tests` for an unlaned single brief without weakening separation of duties

stage: `intent-open`

next:

- `/propose-contracts intent-write-tests-unlaned-brief-b3d8` — _paste_ — intent is open with no live `proposes` candidate
