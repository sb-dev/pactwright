---
id: brief-lane-integration-5e2d
type: brief
title: Lane model, integration node, and coverage-coherence rule (bootstrap single brief)
status: draft
created: 2026-06-20
produced_by: "/write-brief"
---

## Brief: Lane model + integration node + coverage-coherence rule (decomposes contract-lane-integration-convention-body-4c1f)

**Approach recap.** This is the ONE brief decomposing the approved Candidate-A contract (class 3, intent intent-lane-model-integration-a1f7). Per the contract's own single-brief rule and the bootstrap framing, this contract is decomposed by the EXISTING `/write-brief` into exactly this brief; the lane field, `integration` node type, `integrates` edge, the two agents, the three commands, and the `/prepare-evidence` update are all DELIVERABLES that do not yet exist. This single-brief contract SKIPS integration: it has NO real integration node of its own; its lone FINAL evidence completes it and an explicit recorded edit flips intent d4f2 (and a1f7) addressed. The lane/integration machinery built here is exercised only by the SELF-DOGFOOD TEST FIXTURE and by FUTURE class-3 work. The brief delivers the smallest faithful validator surface (one coverage handler) PLUS the human-gated structured-frontmatter enlargement (a second `list_field` rule entry, a third net-new `closed_key_set` handler, a `lane`-enum validity check, an `integration_sections` required field, and a one-integration-per-contract invariant), checking section PRESENCE via FLAT structured frontmatter only — never body prose. One global executable sequence respects within-PR ordering: loader, then schema, then node/edge DATA + capabilities + provenance, then handlers, then the coverage rule is appended after `edges-references-resolve` with a dated cutoff (2026-06-18) so the first validate sees a coherent whole (the 6 pre-cutoff selected contracts created 2026-06-11..06-17 are grandfathered; this contract created 2026-06-19 is judged).

### Files to create

Handlers/loader (capability-spec-tooling-1a2b, paths `[tools/**]`):
- `tools/handlers/coverage_traversal.ts` — shared traversal helper (correction 4), pure functions over `LoadedSpec` + `nodesById` Map. Exports: `liveProposingContracts(spec, byId, intentId)` (verbatim extraction of `liveCandidateSet`, comparison_required.ts lines 36-49 / `liveCandidates`, class_market_quorum.ts lines 26-38 — non-superseded contracts that `proposes` the intent); `intentsForContract(spec, contractId)` (the proposes-walk inlined at comparison_required.ts lines 88-91 and class_market_quorum.ts lines 46-49); a generic `liveSourcesByEdge(spec, byId, edgeType, targetId, excludeStatus='superseded')`. Every endpoint resolution is defensive-skip (returns only resolved, non-superseded nodes). No new behaviour.
- `tools/handlers/coverage_coherence.ts` — net-new `(rule, spec) => Finding[]` default export, kind `coverage_coherence`, modelled on comparison_required.ts. Imports `toDateString` from `../gate.ts`, `nodesById`/`asString` from `../loader.ts`, and the helper above. Reads `spec.coverageCoherenceFrom`; `if (toDateString(cut) === undefined) return []` (fail-open, mirrors comparison_required.ts lines 31-33). Iterates `selects` edges only; resolves the target contract via `nodesById`, defensive-skip unresolved. Grandfathers on the SELECTED CONTRACT's `created`: `const c = toDateString(contract.data['created']); if (c === undefined) return; if (c < cut) return;` (identical predicate to comparison_required.ts lines 83-86). Walks `proposes` to the intent. Computes LIVE BRIEF SET = non-superseded briefs that `decomposes` the contract. COVERING EVIDENCE = `final` evidence (`node.data['status']==='final'`) that `evidences` a brief. Branches: single-live-brief contract covered iff exactly one `final` evidence evidences that lone live brief; multi-live-brief covered iff exactly one `final` `integration` node `integrates` a `final` evidence for EVERY live brief; a multi-brief contract treated covered WITHOUT a final integration is a finding. Bidirectional intent coherence on `intent.data['status']`: finding for addressed-but-uncovered AND covered-but-not-addressed. One-integration-per-contract invariant: distinct finding (subject=contractId) if >1 `integration` integrates evidence for briefs of the same contract. All endpoints via `nodesById`, defensive-skip unresolved. Findings `kind:'coverage_coherence'`, distinct detail strings per branch.
- `tools/handlers/closed_key_set.ts` — net-new ~20-line handler, kind `closed_key_set`, class_range.ts-shaped. Reads `rule.scope`/`rule.type`/`rule.field`/`rule.keys`; emits a config finding to subject `specs/schema/validation-rules.yaml` if `scope!=='nodes'` or type/field/keys missing (mirrors class_range.ts lines 28-37). Reads ONLY `node.data[field]`, NEVER `node.body`. For each node of `type` (integration) whose `field` (integration_sections) is present (skip absent — presence is required_fields' job, class_range.ts line 44 / list_field.ts line 35), assert the list's value-set equals the configured closed key set EXACTLY. Pins the 7 keys: `combined-outputs, combined-test-run, compliance-verdict, rollback-sequencing, combined-risk, follow-ups, scope-integrity`. One finding listing missing/extra (and duplicate) keys. Net-new because enum_constraint.ts hard-returns a config finding on any scope/field beyond nodes/type, edges/type, nodes/status (lines 72-79). The `closed_key_set` handler stays list-only; lane-enum validity is a SEPARATE inline `values:` membership rule (`brief-lane-valid`) in validation-rules.yaml, NOT a scalar mode on this handler.

Agents (capability-lifecycle-commands-4f5a, `.claude/{commands,agents}/**`):
- `.claude/agents/integration-reviewer.md` — judgement-only (frontmatter `tools: Read, Grep`; "writes nothing — graph-maintainer records"), modelled on contract-reviewer.md/spec-critic.md. THE SINGLE CANONICAL declaration of the seven section keys, listed VERBATIM so the byte-equality test can assert byte-equality with the configured `closed_key_set` keys (graft 2). On invocation: locate the contract + every live brief + each brief's FINAL evidence via specs/indexes/; judge whether per-lane FINAL evidence combines to satisfy the contract; apply scope-integrity (CLAUDE.md rule 5); author the integration-node body covering the seven sections AND author the flat top-level `integration_sections` frontmatter list naming exactly those keys; carry the presence-not-substance honesty bound (graft 4 — green declares a well-typed shape, NOT that the combined tests truly ran); hand the drafted node + edge set to graph-maintainer; end with the `pnpm spec:index && pnpm spec:validate` (env form: `node_modules/.bin/tsx tools/spec.ts index && … validate`) mutation reminder.
- `.claude/agents/test-writer.md` — authoring agent that writes/extends a brief's tests under `tests/`, NEVER the same invocation that implemented the code under test (independence invariant sourced from CLAUDE.md rule 2). Locate the test-verification brief via specs/indexes/, write tests, STOP; performs NO graph writes itself (graph writes route through graph-maintainer via /prepare-evidence). Tool set: `Read, Write, Edit` — it may write under `tests/` but is barred from authoring any `specs/` graph file (graph writes route through graph-maintainer via /prepare-evidence).

Commands (same capability):
- `.claude/commands/decompose-lanes.md` — `/decompose-lanes <contract-id> <lane-list>`, modelled on write-brief.md. Locate the contract via specs/indexes/, confirm an inbound `selects` edge (stop/report if absent), read class. Draft one brief per named lane, each carrying its `lane` field + a `decomposes` edge; auto-include a `test-verification` lane brief whenever there is ≥1 implementation lane (CLAUDE.md rule 2). State the laned-brief → implemented → intent-stays-open → /integrate hand-off and the lane-collapse recovery path (a collapsed lane is superseded per CLAUDE.md rule 3, not forced into ceremonial integration). Invoke graph-maintainer; nothing committed on red. Report each brief ID + lane.
- `.claude/commands/write-tests.md` — `/write-tests <brief-id>`, modelled on implement-brief.md. Locate the test-verification brief via specs/indexes/, invoke test-writer against it, independent of /implement-brief; state the expected operator sequence and that /integrate blocks until the test-verification lane reaches FINAL evidence. No graph writes itself.
- `.claude/commands/integrate.md` — `/integrate <contract-id>`, modelled on prepare-evidence.md's closing-report convention + review-contracts.md step 4's replace-not-author idempotency. SINGLE-BRIEF NO-OP (correction 8): print the pinned message ("this contract is single-brief; its lone final evidence completes it — no integration node needed") and exit. IDEMPOTENT/RE-ENTRANT (correction 5): operate on a clean tree; detect an existing draft/partial integration and CONVERGE it (adopt-and-complete the edge set), NEVER author a second; abort with no commit on dirty tree / non-green validate. Invoke integration-reviewer over FINAL per-lane evidence; create exactly one `integration` node with `integrates` edges to each lane's FINAL evidence; write ONLY `integrates` edges, NOT `touches` (correction 3). VERIFY (not author) `touches` coverage authored by /prepare-evidence from per-lane evidence; for any diff path under sensitive_paths (specs/schema/**) confirm the lane evidence's `touches` targets the OWNING capability (capability-spec-schema-2c3d). REFUSE final until every section key is present in `integration_sections` AND every live lane is covered: a partial stays `draft`; when a lane (e.g. the test-verification lane) is not yet at FINAL evidence, converge/keep the `draft` integration and report the blocking lane in the closing report — never refuse to create the node, never author a second integration. Run index + validate; commit only on green. CLOSING REPORT (correction 8): integration ID, draft/final status, which `integration_sections` keys were written, lanes covered vs blocking, the verdict, the partial-integration remediation step.

Tests (capability-spec-tests, NEW dedicated capability):
- `tests/coverage_coherence.test.ts` — node:test, harness shape of comparison_required.test.ts (local `node()`/`spec()` builders, `RULE` const, `CUT='2026-06-18'`); `spec()` sets the new `coverageCoherenceFrom` field. Cases (a)-(k): (a) single-brief + one final evidence + addressed → none; (b) single-brief + draft evidence + addressed → finding; (b2) covered-but-not-addressed → finding; (c) multi-brief + final integration integrating final evidence for every live brief + addressed → none; (d) multi-brief + NO final integration + addressed → finding (L4); (e) final integration integrating only one of two lanes → finding; (f) superseded brief excluded from live set; (g) draft evidence not counted; (h) draft-status integration integrating both lanes → finding (draft-integration boundary); (i) unresolved endpoints defensively skipped, no throw; (j) brief-set-bounded coverage (a stray cross-contract `integrates` edge does not vacuously satisfy coverage — analogue of comparison_required test (i)); (k) multi-brief covered with NO final integration even when each lane has final evidence → finding; PLUS grandfather-on-selected-contract's-created; fail-open on absent/malformed cutoff; one-integration-per-contract invariant; stale-integration row (a live brief superseded AFTER a final integration is caught by the bidirectional covered↔addressed branch — NO new rule, correction 7). Assert `finding.rule/kind/subject/detail`.
- `tests/coverage_traversal.test.ts` — guards the shared-traversal extraction (correction 4): asserts the helper computes the live (non-superseded) brief/candidate set, excludes superseded briefs and draft evidence, resolves endpoints via nodesById skipping unresolved. Standalone, one file per unit.
- `tests/closed_key_set.test.ts` — harness shape of class_range.test.ts. Cases: all 7 keys → none; missing `combined-test-run` → finding naming it; extra/unknown key → finding; duplicate key → finding; field absent → skipped; non-final integration not required to carry the full set; config finding when type/field/keys missing. Asserts the handler reads `node.data` only (a node whose body has the heading text but whose data omits a key still reds).
- `tests/lane_enum.test.ts` — pins lane-enum BEHAVIOUR (correction 7; enum_constraint hard-errors on non-type/status, lines 72-79): brief with lane in the 8-value enum → none; `lane: test-verficiation` (typo) or `lane: testing` → finding; unset lane → none.
- `tests/list_field_integration_sections.test.ts` — focused test that the appended list_field rule (type integration, field integration_sections) reds an empty list and a non-string/empty item, no-op when absent — proving list_field.ts is reused VERBATIM on a second type. Standalone (may fold into an existing list_field test file).
- `tests/fixtures/good-integration/` — whole-tree fixture (sibling of good/, good-drift/, good-waives/): two live laned briefs decomposing one contract, a final evidence per brief, a final integration node with `integrates` edges to both final evidences and a complete 7-key `integration_sections`, intent addressed, `touches` edges sourced from EVIDENCE. The fixture ships committed, pre-generated `specs/indexes/*.yaml` (required by the spec.test.ts byte-identity assertion) and pins its own `coverage_coherence_from` cutoff plus the fixture contract's `created` date so the green case exercises the COVERED branch (not a vacuous grandfather). A new spec.test.ts case asserts index byte-identity + validate exit 0 and by-type groupings for the `integration` node type and `integrates` edge (mirrors good-drift/good-waives by-type assertions).
- `tests/fixtures/bad/uncovered-multi-brief/` (+ expected-errors.txt) — two-live-brief contract, a final evidence per brief, intent addressed, NO final integration node; expected-errors.txt pins the `[rule: coverage-coherence]` finding (L4 self-dogfood RED half).
- `tests/fixtures/bad/two-rules-distinct-kind/` (+ expected-errors.txt) — one graph trips coverage-coherence AND a second distinct-kind rule (integration-sections-keys closed_key_set on an incomplete final integration, OR lane-enum), expected-errors.txt asserts each finding keeps its OWN `[rule:]`/kind prefix and the two are not merged (correction 5/7, also discharges d4f2 distinguishability, correction 2). Mirrors bad/dispatch-all-kinds "one error per rule".

Provenance + capabilities (specs/{nodes}/** — intentionally unowned; see authorization artifact below):
- `specs/nodes/capability-spec-docs-XXXX.md` — NEW dedicated docs capability (correction 4/6), `paths: [CLAUDE.md]`, status active, non-empty body stating it owns the governing-doctrine document. 4-hex suffix at authoring time.
- `specs/nodes/capability-spec-tests-XXXX.md` — NEW dedicated tests capability (correction 4/6), `paths: [tests/**]`, body stating it owns the verification surface. 4-hex suffix at authoring time.
- `specs/nodes/decision-graph-data-unowned-XXXX.md` — durable dated PROVENANCE artifact (correction 2/6): records that `specs/{nodes,graph,indexes}/**` is INTENTIONALLY UNOWNED — who (Samir Benzenine), when (2026-06-20), rationale (graph data, the substrate the coverage rule and check-diff trust). This is what /prepare-evidence's human-confirm branch points at. This dedicated dated decision node is its single home (not folded into a capability body or 9f3b).

### Files to modify

- `specs/schema/node-types.yaml` — (1) document the optional `lane` enum on `brief` (8 values: product-spec | domain-backend | frontend-ui | data-migration | api-integration | test-verification | observability-release | docs-spec; unset = unlaned single brief, allowed) as a COMMENT plus the `brief-lane-valid` rule (NodeTypeDef has no slot for a non-status optional enum and enum_constraint cannot validate it — do NOT add to required_fields). (2) Add node type `integration`: `required_fields [id, type, title, status, created, integration_sections]`, `status_values [draft, final]`, `requires_body: true` (nodes-required-fields then reds a final integration omitting `integration_sections` for free).
- `specs/schema/edge-types.yaml` — add `integrates` with `source: integration`, `target: evidence` (edges-endpoint-types enforces for free). Do NOT widen `touches` (stays source: evidence, target: capability — correction 3).
- `specs/schema/validation-rules.yaml` — append, ALL after `edges-references-resolve` (and after class-market-quorum/comparison-required), in this order: (1) `integration-sections-list` kind list_field, scope nodes, type integration, field integration_sections; (2) `integration-sections-keys` kind closed_key_set, scope nodes, type integration, field integration_sections, keys [the 7]; (3) `brief-lane-valid` lane-enum validity — an inline `values:` membership rule entry naming the 8 lane values inline (class_range-style), avoiding a loader/NodeTypeDef change; (4) `coverage-coherence` kind coverage_coherence, appended after `edges-references-resolve` (and after class-market-quorum / comparison-required); its position relative to `indexes-fresh` is immaterial since findings don't short-circuit; the ordering test asserts coverage-coherence is listed AFTER `edges-references-resolve`. Add top-level scalar `coverage_coherence_from: "2026-06-18"` directly below `comparison_required_from` (lines 79-83) with a parallel comment. Keep `sensitive_paths` at `specs/schema/**` ONLY (correction 4).
- `tools/validator.ts` — import and register in HANDLERS (lines 25-36): `coverage_coherence: coverageCoherence`, `closed_key_set: closedKeySet`, and the lane-enum kind if a dedicated handler is chosen. Dispatch-pinning (correction 3, graft 3): every new rule kind MUST resolve in HANDLERS — runValidation hard-fails an unknown kind (lines 69-78). No change to runValidation/writeReport.
- `tools/loader.ts` — add `coverageCoherenceFrom?: string` to `LoadedSpec` (alongside lines 48-51); in loadSpec read `rulesDoc['coverage_coherence_from']` via `asString(...)` exactly like line 137; include it in the returned object (line 148). Top-level scalar, mirrors comparison_required_from. (Existing full-`LoadedSpec` test builders compile unchanged because the field is optional.)
- `tools/handlers/comparison_required.ts` — refactor to consume coverage_traversal.ts (correction 4): replace inlined `liveCandidateSet` (lines 36-49) and the proposes-walk (lines 88-91) with helper calls. Behaviour-preserving; comparison_required.test.ts stays green.
- `tools/handlers/class_market_quorum.ts` — refactor to consume coverage_traversal.ts (correction 4): replace inlined `liveCandidates` (lines 26-38) and the proposes-walk (lines 46-49). Behaviour-preserving; class_market_quorum.test.ts stays green.
- `.claude/commands/prepare-evidence.md` — update for the laned lifecycle: (a) laned brief → `implemented` but intent stays `open` (intent reaches `addressed` only via /integrate); unlaned single brief unchanged. (b) Capability wiring: map diff to capability paths[] globs from specs/indexes/by-type.yaml; add `touches` edges FROM THE NEW EVIDENCE (source=evidence) to every owning capability; bind sensitive-path (specs/schema/**) touches to the OWNING capability (correction 3); STOP-and-ask when the diff touches unowned paths — the prompt ENUMERATES unowned paths, LISTS extendable capabilities with current globs, and names how each of the three resolutions is recorded (extend / create via /update-spec-graph / confirm intentionally-unowned, the last persisting the durable dated artifact, correction 6). Because `specs/nodes/**` (and `specs/{graph,indexes}/**`) is confirmed intentionally-unowned, /prepare-evidence does NOT STOP-and-ask for the new capability/decision node files this PR adds under `specs/nodes/`. IDEMPOTENT/RE-ENTRANT (correction 5): clean tree; on resume detect already-written evidence and only complete missing touches wiring; commit only on green. The capability map now resolves CLAUDE.md → the new docs capability and tests/** → the new tests capability (NOT 1a2b). Keep the existing closing report.
- `CLAUDE.md` — add (1) the 8-lane catalog and what each owns; (2) the 4 lane rules (class-3 decomposes into lanes / Class 2 may; verification is its own lane owned by test-writer; single-brief skips integration, multi-lane completed by a final integration node = coverage artifact + release `includes` target; every evidence records `touches`, an unowned-path diff is a same-PR coverage gap); (3) the HONEST BOUND (graft 4): green asserts the integration node exists, is wired to final evidence for every live brief, and DECLARES the required sections of a well-typed shape — NOT that the combined tests ran; green is never over-read as "verified"; (4) a POINTER naming `.claude/agents/integration-reviewer.md` as the SINGLE canonical section vocabulary (CLAUDE.md references it, does not copy the list). The single-canonical-vocabulary pointer line (to `.claude/agents/integration-reviewer.md`) is authored exactly once, in CLAUDE.md, so the byte-equality target is unambiguous. Surface the laned-brief → open → /integrate hand-off. Mirror the existing comparison-body convention paragraph (lines 133-141) in tone.
- `specs/nodes/intent-status-coherence-d4f2.md` — EXPLICIT manual frontmatter edit `status: open → addressed` (correction 2; d4f2 has zero incoming edges — confirmed: only a by-type.yaml listing, no edge references it — so the selects-scoped rule structurally cannot reach it). Authorized by decision-lane-integration-9f3b (the durable provenance record). Sequenced AFTER this PR's coverage rule + final evidence land. No supersede, no new intent (Non-scope).
- `specs/nodes/capability-spec-tooling-1a2b.md` — explicit NON-annexation: do NOT widen its `paths` to add CLAUDE.md / tests/** (the decision overrides the contract's original Scope annexation in favour of two dedicated capabilities). Stays `paths: [tools/**]`. Listed only to record the non-annexation.

### Libraries / test entries
- No new dependencies. Tests use the existing `node:test` + `node:assert/strict` harness; the suite runs via `npm test` (`node --test --import tsx tests/*.test.ts`) — new `tests/*.test.ts` files are auto-discovered. Real-tree checks: `node_modules/.bin/tsx tools/spec.ts index && node_modules/.bin/tsx tools/spec.ts validate`. Reused utilities (no new files): list_field.ts (verbatim, completeness floor), class_range.ts (shape template for closed_key_set), required_fields.ts + requires_body (integration presence/body for free), enum_constraint.ts (integration.status for free; the reason lane-enum/closed_key_set are net-new), edge_endpoint_types.ts (integrates + touches endpoints for free), gate.ts `toDateString`, loader.ts `nodesById`/`asString`/`capabilityPaths`.

### ORDERED implementation steps (one global executable sequence; within-PR ordering — schema + data + capabilities land BEFORE the coverage rule is appended)
1. LOADER FIRST: add `coverageCoherenceFrom?: string` to `LoadedSpec` and read `coverage_coherence_from` in loadSpec (tools/loader.ts), mirroring comparison_required_from line 137. Nothing can consume the cutoff until the loader surfaces it as a named field (correction 1).
2. SCHEMA node-types.yaml: add the `integration` node type (required_fields incl. integration_sections, status_values [draft,final], requires_body true) and document the optional `lane` enum comment on brief. nodes-required-fields + requires_body + nodes-status-in-enum then cover integration presence/body/status for free.
3. SCHEMA edge-types.yaml: add `integrates` (integration→evidence). edges-endpoint-types covers it for free; leave `touches` source=evidence untouched (correction 3).
4. CAPABILITIES + PROVENANCE (data lands before the rule judges): author capability-spec-docs-XXXX (paths [CLAUDE.md]) and capability-spec-tests-XXXX (paths [tests/**]); explicitly do NOT widen 1a2b; author decision-graph-data-unowned-XXXX (the durable dated unowned-graph-data authorization). Keep sensitive_paths at specs/schema/** only.
5. Extract tools/handlers/coverage_traversal.ts (shared live-proposing-contracts / live-sources-by-edge / intents-for-contract) BEFORE refactoring consumers (correction 4).
6. Refactor comparison_required.ts and class_market_quorum.ts to consume coverage_traversal.ts; run their existing tests to confirm behaviour-preserving.
7. Write tools/handlers/closed_key_set.ts (class_range-shaped, node.data only, pins the 7 keys). Wire lane-enum validity as a separate inline `values:` membership rule (`brief-lane-valid`); the `closed_key_set` handler stays list-only. Register `closed_key_set` (and the lane-enum kind) in tools/validator.ts HANDLERS.
8. Write tools/handlers/coverage_coherence.ts: fail-open on cutoff; selects-scoped; grandfather on selected-contract's created; single-brief covered iff one final evidence; multi-brief covered iff one final integration integrates a final evidence for every live brief; bidirectional intent coherence; one-integration-per-contract invariant; defensive endpoint skip. Register `coverage_coherence` in HANDLERS.
9. APPEND rules to validation-rules.yaml in order: integration-sections-list (list_field), integration-sections-keys (closed_key_set), brief-lane-valid (lane enum), coverage-coherence. The coverage-coherence rule is appended after `edges-references-resolve` (and after class-market-quorum / comparison-required); its position relative to `indexes-fresh` is immaterial since findings don't short-circuit; the ordering test asserts coverage-coherence is listed AFTER `edges-references-resolve`. Add top-level `coverage_coherence_from: "2026-06-18"` below comparison_required_from. This is the within-PR ordering (correction 1): the rule is appended only after this PR's own integration/touches/capability-paths data lands, so the first validate sees a coherent whole; the 6 pre-cutoff selected contracts (06-11..06-17) are grandfathered, this contract (06-19) is judged.
10. Author the two agents (.claude/agents/integration-reviewer.md FIRST as the single canonical 7-key vocabulary; then test-writer.md with its pinned tool set) and the three commands (decompose-lanes.md, write-tests.md, integrate.md); update prepare-evidence.md (laned status, evidence-sourced touches with sensitive-path-to-owning-capability binding, the fully specified STOP-and-ask prompt content, re-entrant clean-tree idempotency, capability map → docs/tests capabilities).
11. Update CLAUDE.md: 8-lane catalog, 4 lane rules, the honest bound, the pointer to integration-reviewer.md as single vocabulary, the laned-brief → open → /integrate hand-off.
12. Write the unit test suites: coverage_coherence.test.ts (cases a-k + grandfather + fail-open + one-integration invariant + stale-integration row), coverage_traversal.test.ts, closed_key_set.test.ts, lane_enum.test.ts, list_field_integration_sections.test.ts.
13. Write the whole-tree fixtures + spec.test.ts cases: good-integration/ (green, by-type assertions for integration + integrates; ships committed, pre-generated `specs/indexes/*.yaml` required by the spec.test.ts byte-identity assertion, and pins its own `coverage_coherence_from` cutoff + the fixture contract's `created` date so the green case exercises the COVERED branch — not a vacuous grandfather), bad/uncovered-multi-brief/ (+ expected-errors.txt, L4 RED half), and the self-dogfood red-then-green case (a spec.test.ts mutate-fixture-mid-test like bad/schema-extension: validate uncovered-multi-brief RED, then add the final integration + integrates + 7-key integration_sections, re-validate GREEN — correction 7e). bad/two-rules-distinct-kind/ asserting each finding keeps its own [rule:]/kind prefix.
14. Add the dispatch-pinning assertion (graft 3, correction 7): assert each new rule kind resolves in HANDLERS and emits on a known-bad fixture (mirror bad/dispatch-all-kinds), and an ordering assertion that coverage-coherence is listed after `edges-references-resolve` (its position relative to `indexes-fresh` is immaterial since findings don't short-circuit). Add the byte-equality test asserting integration-reviewer.md's documented 7-key set equals the configured closed_key_set keys (graft 2).
15. EXPLICIT manual d4f2 edit: set intent-status-coherence-d4f2 status open → addressed (provenance: decision-lane-integration-9f3b), sequenced after the coverage rule + final evidence land.
16. Confirm free-schema checks via the real CLI in good-integration: integration with `status: review` fails nodes-status-in-enum; empty-body integration fails requires_body; an integrates edge from a non-integration source / to a non-evidence target fails edges-endpoint-types (confirm-do-not-reimplement). Run the full `npm test` + real-tree `index && validate` green.

### Non-scope (explicit)
- NO integration-BODY/prose validation and NO markdown/heading parser — node.body is never read by any new handler; section presence is machine-checked ONLY via the structured `integration_sections` frontmatter list (do NOT graft B's body-prose parser, its heading-match/fenced-code/whitespace semantics, or the per-type body-validation doctrine break).
- NO nested-object validation — `integration_sections` is a FLAT top-level string list (no nested-object precedent).
- NO widening of the `touches` edge source type — touches stays source=evidence (correction 3); authoring touches-from-evidence is /prepare-evidence's job.
- NO new CI workflow — the coverage rule rides the existing spec-validate.yml.
- NO supersede / new intent for d4f2 and no auto-rule driving d4f2 open→addressed — the selects-scoped rule structurally cannot reach d4f2 (zero edges); the transition is the explicit decision-recorded manual edit.
- NO real integration node for THIS contract — this single-brief contract SKIPS integration; the lane/integration machinery is exercised by the self-dogfood TEST FIXTURE, not a live integration node for this PR.
- NO change to runValidation/writeReport control flow or to existing rule ordering before edges-references-resolve.
- NO unresolved.yaml / report-routing for the coverage rule — it is hard-fail Finding[] (d4f2's unresolved.yaml/distinguishability is deliberately replaced by hard-fail distinct-kind findings, correction 2/graft 4).
- NO release-node / `includes`-edge schema work; NO widening of sensitive_paths (stays specs/schema/** only); NO retire/supersede lifecycle changes beyond scope-integrity.
- NOT in this brief's authoring scope but named where they intersect: corrections 5/6/8 command-message pins live in the command bodies; the d4f2 manual edit and the unowned-graph authorization are graph-data; the lane catalog body is CLAUDE.md.

### Verification (commands + acceptance mapping)
- `npm test` (node --test --import tsx tests/*.test.ts) — runs all unit suites + spec.test.ts fixtures. Acceptance: coverage_coherence.test.ts (a)-(k) cover L1 (multi-lane can't be addressed without a final integration over every lane), the d4f2 single-lane subsumption special case, and the status-safety trap; lane_enum/closed_key_set/list_field suites cover the structured-frontmatter resolution; the byte-equality test covers graft 2; the dispatch-pinning assertion covers graft 3.
- `node_modules/.bin/tsx tools/spec.ts index && node_modules/.bin/tsx tools/spec.ts validate` on the real post-migration tree — GREEN. Acceptance: real-tree green including this PR's own evidence/touches wiring and the new capabilities; the 6 grandfathered contracts (06-11..06-17 < 06-18) stay green; this contract (06-19) is judged.
- spec.test.ts good-integration → index byte-identity + validate exit 0; bad/uncovered-multi-brief → exit 1 with `[rule: coverage-coherence]` (L4); self-dogfood red-then-green case proves the rule fires on an incomplete graph and clears when the integration is added (correction 7e); bad/two-rules-distinct-kind proves distinct kinds (L-d4f2 distinguishability); ordering assertion proves coverage-coherence is after edges-references-resolve.
- L3 (/prepare-evidence STOP-and-ask on an unowned path) is exercised by the command body + the /prepare-evidence capability-map update (CLAUDE.md → docs capability, tests/** → tests capability, specs/{nodes,graph,indexes}/** intentionally unowned).
