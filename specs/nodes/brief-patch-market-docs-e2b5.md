---
id: brief-patch-market-docs-e2b5
type: brief
title: Patch-market governance docs — CLAUDE.md patch-market section, by-class row, synthesis-vs-integration, scope-integrity in patch review
status: draft
created: 2026-06-24
lane: docs-spec
produced_by: "/decompose-lanes"
---
This brief decomposes `contract-patch-market-ci-gate-6b7e` (status: approved, class 3) for the 'docs-spec' lane of `intent-patch-market-synthesis-3b1e`, per decision `decision-patch-market-ci-gate-8a2f`. This lane owns the governing-doctrine prose only: it adds to `CLAUDE.md` the patch-market doctrine — per-lane operation and lane-isolation, the by-class statement, within-lane synthesis vs across-lane integration, and that patch review carries the Phase-6 scope-integrity rules — and touches no code, schema, command, workflow, or test.

## Grounding (reuse, don't reinvent)

This lane writes governance prose into one file, so its "reuse" is the existing `CLAUDE.md` structure and the surrounding lane's machinery the prose describes (named, not authored, here):

- **The sole file this lane owns** — `/home/samir/workspace/pactwright/CLAUDE.md`. It is itself the dedicated docs capability's path (`capability-spec-docs-*`, `paths: [CLAUDE.md]`, established by `brief-lane-integration-5e2d`). No other lane in this decomposition edits it, so this lane's diff is `CLAUDE.md`-only and its `touches` resolves cleanly to that one capability.
- **The work-class routing table already has a `Patch market` column** — `CLAUDE.md` lines 89–94. Its cells already read `none` (Class 0), `none` (Class 1), `optional per brief` (Class 2), `available per lane` (Class 3). The by-class statement this lane adds (graft (b)) must **integrate with the existing column**, not add a second one: the prose elaborates the column's four cells, and the cells stay as they are. This mirrors how the table's other columns (`Critics`, `Lanes`) are elaborated by the `### Critic routing` and `## Lane model and integration` subsections below it.
- **The `### Critic routing` subsection** — `CLAUDE.md` lines 104–130. Its voice (bold lead-ins, `spec-critic plus …`, the "no concern on this axis" / "never read as a clean bill" closing convention) is the model for tone; the new patch-market doctrine sits as a sibling subsection in the same register.
- **The `### Proposal comparison` subsection** — `CLAUDE.md` lines 132–155. The patch market is the per-lane, per-brief analogue of the contract-level proposal market it describes (`one comparison node per market`, `compares` edges, `never superseded by selection`, `replaces … rather than authoring a second`). The patch-market prose should explicitly mirror this convention so a reader sees patch comparison as "proposal comparison, but scoped to one lane brief instead of one intent."
- **The `## Lane model and integration` section** — `CLAUDE.md` lines 157–206. It already states the lane catalog, that verification is its own lane, that single-brief contracts skip integration, and that **across-lane combination is integration** (the `integration` node is "the contract's coverage artifact"). The patch-market doctrine's graft (c) — within-lane synthesis is a `synthesizes`-edged synthesis patch, across-lane combination is integration — dovetails with this section: synthesis is the within-lane operation, integration (already documented here) is the across-lane one. The prose names the existing `integration` machinery rather than re-describing it.
- **Scope-integrity rule 5** — `CLAUDE.md` lines 52–62. It already enumerates "Whenever any review (contract, **patch**, or integration) reveals the approved contract or brief was wrong" and its three branches (supersede the brief; capture a follow-up intent; return to human approval). The word "patch" is **already present** in that rule's lead-in. Graft (d) — patch review carries the Phase-6 scope-integrity rules — is therefore satisfied by a **pointer** from the patch-market subsection back to rule 5, not by restating the three branches. This is the established CLAUDE.md convention (the `## Lane model` honest-bound paragraph points to `integration-reviewer.md` rather than copying the key list).
- **The schema/command/workflow vocabulary the prose references** — authored in *other* lanes, named here for correctness: the `patch` node type and `competes-for`/`synthesizes` edges (data-migration lane → `node-types.yaml`/`edge-types.yaml`); the four commands `/propose-patches`, `/compare-patches`, `/synthesize-patches`, `/select-patch` (api-integration lane); the `patch-comparison.yml` workflow and `patch-comparison` named check (observability-release + data-migration lanes). This lane must spell these identifiers byte-correctly but creates none of them.

## Pinned decisions (this lane's slice of the grafts/fixes — binding constraints)

- **CLAUDE.md only; governance prose only.** This lane adds doctrine; it writes no code/schema/command/workflow/test. The mechanism the prose describes (the dedicated diff-aware `spec:patch-gate`, the `patch-comparison.yml` workflow, the two structural validate rules, the four commands) is built in the data-migration, domain-backend, api-integration, observability-release, and test-verification lanes. The prose must describe the **as-built** mechanism those lanes ship, not a divergent one.
- **Integrate with the existing `Patch market` column, do not duplicate it.** The by-class statement (graft (b)) elaborates the table's existing four cells (`none` / `none` / `optional per brief` / `available per lane`) in prose; it does not add a column and does not change the cell values.
- **Lane isolation is the load-bearing doctrine (graft (b)/(c) and Candidate B's defining frame).** The prose must state: the patch market runs **per lane brief**; patches compete **within one lane**; patch comparison judges **that lane in isolation**; **cross-lane fit is judged at integration, never in patch comparison**. This is the boundary the whole Candidate-B design rests on (a per-brief gate that fires on the lane's merge, never a whole-graph judgement).
- **Within-lane synthesis vs across-lane integration (graft (c)).** Within-lane combination of competing patches is a **synthesis patch** (`patch/<brief-slug>/synthesis`) carrying `synthesizes` edges to each parent (and a `competes-for` to the same lane brief); across-lane combination is the **`integration` node** already documented in `## Lane model and integration`. The prose must keep these two operations cleanly separate — a synthesis patch never crosses lanes.
- **Patch review carries Phase-6 scope-integrity (graft (d)).** The patch-market subsection points to scope-integrity rule 5 (which already names "patch" review) rather than restating its branches: a patch comparison that reveals the contract or brief was wrong supersedes the brief, captures a follow-up intent, or returns to human approval — it never widens scope silently inside the winning patch.
- **Honest framing of the gate's bound (consistency with the contract and the `## Lane model` honest-bound convention).** Where the prose mentions the merge gate, it must not over-claim: a green `patch-comparison` check asserts that a `comparison` node and a `selects` decision **exist** covering the lane's live competitors (or a non-expired `waives → patch-comparison` override is present), not that the comparison was substantive — mirroring the existing "Honest bound" paragraph for integration. (Detailed gate semantics, the override path, the fail-closed PR→brief mapping, and the dated cutoff live in the domain-backend / data-migration / observability-release lanes' prose and schema; this lane states only the doctrine a reader needs, and defers mechanism to those lanes.)
- **Scope-integrity for this lane's own boundary (CLAUDE.md rule 5).** If, while drafting, this prose would have to assert a mechanism that contradicts what another lane actually ships (e.g. a different by-class rule than the table column, or a synthesis-crosses-lanes claim), stop — that is a contract/decision drift signal, not a prose fix. Surface it; do not silently encode a divergent doctrine.

## Files to create

None. This lane creates no files.

## Files to modify

- `/home/samir/workspace/pactwright/CLAUDE.md` — the only file this lane touches.

## Ordered implementation steps

All steps edit `/home/samir/workspace/pactwright/CLAUDE.md` only. Line numbers below are current-tree anchors; **re-confirm each before editing, since earlier edits in the same file shift them.** The recommended placement is a new `### Patch market` subsection that sits alongside `### Critic routing` and `### Proposal comparison`, with a short pointer added to the `## Lane model and integration` section. The patch-market doctrine logically belongs near the lane model (it is per-lane-brief), so author the subsection at the end of `## Lane model and integration` (after the honest-bound paragraph, current line 206) OR as a `### Patch market` subsection immediately following `### Proposal comparison` (current line 155); pick whichever keeps the document's existing top-to-bottom flow (lane model is the better home, since the doctrine is lane-scoped).

1. **Confirm the existing `Patch market` column values are unchanged** (current `CLAUDE.md` lines 89–94: `none` / `none` / `optional per brief` / `available per lane`). Do **not** edit the table cells. The by-class prose in step 3 elaborates these existing cells. (This is a read/confirm step; it produces no edit, but it pins the by-class prose to the table's authority.)

2. **Add the per-lane / lane-isolation paragraph (graft (b) core).** State, in CLAUDE.md's voice: the patch market runs **per lane brief**; multiple candidate patches compete **within a single lane**; patch comparison judges **that lane in isolation** against the lane brief's contract slice; **cross-lane fit is never judged in patch comparison — it is judged at integration** (cross-reference the `integration` node already documented in `## Lane model and integration`). Make explicit that a patch market is opened by `/propose-patches <brief-id>` on a lane brief (or a single brief), sets `patch_market: true` on that brief, and creates one `patch` node per strategy with a `competes-for` edge to that brief.

3. **Add the patch-market-by-class statement (graft (b)).** Elaborate the existing `Patch market` table column in prose, as a sibling to how `### Critic routing` elaborates the `Critics` column:
   - **Class 0–1** — a single patch, **no market** (one implementation, no competing candidates; matches the table's `none` cells).
   - **Class 2** — a patch market is **optional per brief** (matches `optional per brief`).
   - **Class 3** — a patch market is **available per lane** (matches `available per lane`).
   Reference the existing column explicitly ("the `Patch market` column of the work-class routing table") so the reader sees the prose and the table as one statement, not two.

4. **Add the within-lane synthesis vs across-lane integration paragraph (graft (c)).** State: within a lane, competing candidate patches may be combined into a **synthesis patch** — a `patch` on branch `patch/<brief-slug>/synthesis` carrying `synthesizes` edges to each parent patch it combines and a `competes-for` edge to the **same** lane brief — authored by `/synthesize-patches`. Across-lane combination is **never synthesis**: it is the `integration` node already documented above (the contract's coverage artifact over every live lane). Keep the two crisply separated — synthesis is within one lane, integration is across lanes. Note the parentage invariant in passing (a synthesis patch combines ≥2 parents; the structural `synthesis_parentage` rule, built in the domain-backend lane, enforces it) without specifying the rule's mechanics.

5. **Add the patch-comparison-as-durable-record sentence, mirroring `### Proposal comparison`.** State that `/compare-patches` records exactly **one** `comparison` node per lane market (`comparison —compares→ patch`, one edge per live competitor), it is the durable record the `/select-patch` `decision` cites, it is **never superseded by selection**, and re-running `/compare-patches` **replaces** it rather than authoring a second — the per-brief analogue of the contract-level proposal comparison. This explicitly reuses the `### Proposal comparison` convention rather than inventing a parallel one.

6. **Add the scope-integrity pointer (graft (d)).** Add one sentence stating that **patch review carries the Phase-6 scope-integrity rules**: a patch comparison that reveals the approved contract or brief was wrong follows scope-integrity rule 5 (supersede the brief, capture a follow-up intent, or return to human approval) — it never widens scope silently inside the winning patch. Point back to rule 5 (lines 52–62, which already names "patch" review) rather than restating its three branches.

7. **Add a one-line honest-bound note where the merge gate is mentioned** (consistency with the existing "Honest bound" paragraph). State that a green `patch-comparison` check asserts a covering `comparison` and `selects` (or a non-expired `waives → patch-comparison` override) **exist** for the lane's live competitors — not that the comparison was substantive. Defer the gate's full mechanism (fail-closed PR→brief mapping, override expiry, the dated wiring) to the lanes that build it, with a one-clause pointer ("enforced by the `patch-comparison` CI gate; see the domain-backend and observability-release lanes' artifacts").

8. **Re-read the edited section end-to-end for voice and cross-reference correctness.** Confirm: the `Patch market` table column is unchanged; every identifier (`patch`, `competes-for`, `synthesizes`, `patch_market`, `comparison`, `compares`, `selects`, `/propose-patches`, `/compare-patches`, `/synthesize-patches`, `/select-patch`, `patch-comparison`, `waives → patch-comparison`, `synthesis_parentage`) is spelled exactly as the other lanes' artifacts spell it; the synthesis/integration boundary reads cleanly against `## Lane model and integration`; the scope-integrity pointer resolves to rule 5. This lane authors **no** `spec:index`/`spec:validate` mutation of its own — `CLAUDE.md` is prose outside the validated graph and no rule reads it — but the graph write that records this brief's provenance (the `decomposes` edge) and this lane's eventual evidence do run the mutation gate; see Cross-lane dependencies.

## Non-scope (explicit — the other lanes' files/work, per the six-lane boundary map)

- **No schema** — `specs/schema/node-types.yaml`, `specs/schema/edge-types.yaml`, `specs/schema/checks.yaml` (the `patch` node, `competes-for`/`synthesizes` edges, widened `compares`/`selects` targets, the `patch_market` brief flag, the `patch-comparison` named check) are the **data-migration** lane's. This lane references these identifiers in prose but defines none of them.
- **No `tools/**` and no validation-rules declarations** — `tools/patch_gate.ts`, the two new handlers, the shared live-competitor predicate, `tools/spec.ts`, `tools/validator.ts`, `package.json`, and `specs/schema/validation-rules.yaml` rule **declarations** (`synthesis_parentage`, selected-patch comparison coverage) are the **domain-backend** lane's. Panel Fixes 1–3 (type-guard `intentsForContract` callers; exclude `selected` from the live set; graph-first fail-closed PR→brief mapping) and Grafts A+C's gate core live there. This lane states the doctrine; it implements no gate.
- **No commands** — `.claude/commands/{propose-patches,compare-patches,synthesize-patches,select-patch}.md` (and Fix 4, the closing status-report contract) are the **api-integration** lane's.
- **No CI / branch-protection** — `.github/workflows/patch-comparison.yml` and the branch-protection / required-check wiring docs (Fix 5, Graft C's fire-on-code-merge) are the **observability-release** lane's. This lane mentions the `patch-comparison` check only as doctrine; it does not author the workflow or document the wiring.
- **No tests** — `tests/**` (Fix 2/3 coverage, the gate/handler parity test) are the **test-verification** lane's, owned by `test-writer` via `/write-tests`, never the implementing invocation.
- **Within this lane:** no edit to any `CLAUDE.md` line outside the new patch-market subsection and the one scope-integrity pointer — in particular, the `Patch market` table column cells are **not** changed (the prose elaborates them; the table stays authoritative), and the existing `### Critic routing`, `### Proposal comparison`, and `## Lane model and integration` text is not rewritten beyond adding the new subsection and any single cross-reference pointer.

## Cross-lane dependencies & integration expectation

- **This laned brief reaches `implemented`** via this `docs-spec` lane's own **final `evidence`** (`evidence —evidences→ brief`), while `intent-patch-market-synthesis-3b1e` stays **`open`**. A laned brief's evidence implements the brief but does not address the intent; the intent reaches `addressed` only via integration (the `coverage-coherence` rule).
- **The contract `contract-patch-market-ci-gate-6b7e` completes only via a final `integration` node** (`/integrate`) that `integrates` a final evidence for **every live lane** — `data-migration`, `domain-backend`, `api-integration`, `observability-release`, `docs-spec`, and `test-verification`. This six-lane change is multi-brief, so it does **not** skip integration; per `coverage-coherence` it cannot mark the intent `addressed` until that final integration covers every live lane.
- **If a lane collapses** (its work proves empty or folds into another), it is **superseded** per CLAUDE.md rule 3 — a `supersedes` edge from a corrected/empty record, with the collapsed brief moved to its terminal status — **not** forced into ceremonial integration. The integration node covers only the lanes that remain live.
- **What this lane depends on (for correctness of its prose, not for its own commit):** the **as-built** identifiers and mechanism shipped by the data-migration lane (`patch`, `competes-for`, `synthesizes`, `patch_market`, `patch-comparison` check), the domain-backend lane (the `patch-comparison` gate semantics, `synthesis_parentage` rule), the api-integration lane (the four command names), and the observability-release lane (the `patch-comparison.yml` workflow). The prose must match what those lanes actually ship; if it cannot, that is a drift signal to surface (CLAUDE.md rule 5), not a prose patch. This lane's *file* (`CLAUDE.md`) has no build-order dependency — it can be authored in parallel — but its *content* must be reconciled against the other lanes' artifacts before its evidence is final.
- **What depends on this lane:** the `integration-reviewer` at `/integrate` reads this lane's final evidence as one of the six required lane coverages; the contract cannot complete without it. No other lane's *code* imports this prose, but the patch-market commands and the merge gate the other lanes ship are the operational expression of the doctrine this lane writes, so reviewers will read this section to confirm the shipped mechanism matches the governing rules.

## Acceptance & verification (scoped to this lane)

This lane is governance prose; it has no machine check of its own (no validate rule reads `CLAUDE.md`, mirroring how `### Critic routing` and the lane catalog are unenforced free text). Its acceptance maps to the contract's CLAUDE.md scope item (contract `## Scope`, the `CLAUDE.md` bullet) and acceptance examples 1–5 only in the sense that the doctrine this lane writes must correctly *describe* the mechanism those examples exercise. The test-verification lane owns all actual test code; this lane states what its prose must satisfy:

- **By-class fidelity (graft (b); contract Scope).** The added by-class prose names Class 0–1 = single patch / no market, Class 2 = optional per brief, Class 3 = available per lane, and these **agree with the existing `Patch market` column** of the work-class routing table (no column added, no cell changed). Verified by direct read of the edited section against `CLAUDE.md` lines 89–94.
- **Lane-isolation fidelity (graft (b)/(c); Candidate B's defining frame).** The prose states the market runs per lane brief, patches compete within one lane, patch comparison judges that lane in isolation, and cross-lane fit is judged at integration — never in patch comparison. Verified by read against the contract's problem interpretation and `## Lane model and integration`.
- **Synthesis vs integration fidelity (graft (c); contract acceptance example 5).** The prose distinguishes a within-lane `synthesizes`-edged synthesis patch (on `patch/<brief-slug>/synthesis`, competing for the same lane brief) from across-lane integration, and names the `synthesis_parentage` ≥2-parent invariant without specifying its mechanics. Verified by read against acceptance example 5 and the intent's CLAUDE.md additions list (intent lines 50–58).
- **Scope-integrity fidelity (graft (d)).** The prose points patch review at scope-integrity rule 5 (supersede the brief / capture a follow-up intent / return to human approval; never widen silently), consistent with rule 5 already naming "patch" review. Verified by read against `CLAUDE.md` lines 52–62.
- **Honest-bound fidelity.** Where the merge gate is mentioned, the prose claims only that a covering `comparison` + `selects` (or a non-expired override) **exist**, not that the comparison was substantive — consistent with the existing "Honest bound" paragraph and the contract's own honesty framing. Verified by read against `CLAUDE.md` lines 196–206 and the contract's product/qa critiques on the presence-not-substance bound.
- **Identifier byte-correctness.** Every schema/command/workflow identifier the prose names is spelled exactly as the data-migration, domain-backend, api-integration, and observability-release lanes ship it (`patch`, `competes-for`, `synthesizes`, `patch_market`, `comparison`, `compares`, `selects`, the four `/…-patches` commands, `patch-comparison`, `waives → patch-comparison`, `synthesis_parentage`). Verified by cross-reading those lanes' artifacts before this lane's evidence is finalized.
- **No graph mutation from the prose itself.** Authoring `CLAUDE.md` triggers no `spec:index`/`spec:validate` change (it is outside the validated graph). The only graph writes for this brief are its provenance `decomposes` edge and, later, its `evidence`/`touches` wiring — and **those** mutating steps end with `pnpm spec:index && pnpm spec:validate` (in this PRoot env, `node_modules/.bin/tsx tools/spec.ts index` then `… validate`) and **must not commit on failure**. The diff being `CLAUDE.md`-only, this lane's evidence `touches` resolves to the dedicated docs capability (`capability-spec-docs-*`, `paths: [CLAUDE.md]`).

Edge for graph-maintainer to record for this brief node: `brief —decomposes→ contract-patch-market-ci-gate-6b7e`.