---
id: decision-lane-integration-9f3b
type: decision
title: Select the convention-body lane-model contract (A) with a recorded structured-frontmatter body refinement
decided_by: Samir Benzenine
created: 2026-06-20
produced_by: "/approve-contract"
---

SELECTED (-> approved): contract-lane-integration-convention-body-4c1f (Candidate A, convention body). REJECTED: contract-lane-integration-validated-body-7b2e (Candidate B, machine-validated body).

This is a clean step-3 selection in the decision-work-class-routing-4d7a mould: one base approved, the corrections below recorded (not silently absorbed) and binding the implementation brief, with the discipline/honesty grafts taken from the rejected candidate. None of the corrections change the intent's intended behaviour; the one scope refinement that exceeds A's literal Non-scope was raised and HUMAN-CONFIRMED at selection (bake it into this decision rather than a follow-up intent).

WHY A (not B): the two candidates ship an identical core and differ on exactly one axis - does spec:validate machine-check the integration body. B closes A's gap by adding the codebase's FIRST markdown-prose parser inside the merge gate. Confirmed against tools/loader.ts and every handler: the validator reads ONLY frontmatter (node.data) + edges; the sole body-touching code is required_fields.ts's body.trim()===''. B's parser is therefore a permanent novel maintenance surface that (i) false-reds on heading drift/whitespace/fenced code and blocks correct multi-lane merges, (ii) false-greens on spoofed/hollow sections, (iii) scatters the required-heading vocabulary across three hand-synced files, (iv) checks presence not substance (B's own example 8), and (v) self-blocks: its first subject is this PR's own hand-authored integration body. That cost is not justified by a presence-only guarantee.

BODY-COMPLETENESS RESOLUTION (the mix - close A's gap WITHOUT a prose parser): A's honest weakness is that the single most consequential fact (was the combined whole verified) has NO machine signal - a one-character body passes. Close it with STRUCTURED FRONTMATTER: promote the seven required sections into a FLAT top-level integration_sections string list (keys: combined-outputs, combined-test-run, compliance-verdict, rollback-sequencing, combined-risk, follow-ups, scope-integrity). nodes-required-fields reds a final integration that omits the field for free; one new list_field rule reuses list_field.ts VERBATIM (proven on capability.paths) for the completeness floor; one net-new ~20-line closed-key-set handler (class_range-shaped, reads only node.data, never node.body) pins the exact seven keys. The list MUST be flat - there is no nested-object validation precedent. The key-enum handler is genuinely net-new (NOT free) because enum_constraint.ts handles only type/status and HARD-ERRORS on any other scope/field combo. /integrate refuses final until every key is present and echoes a closing report of what it wrote; the integration-reviewer agent definition is the SINGLE canonical section vocabulary, with CLAUDE.md pointing to it. The narrative body stays convention under requires_body. This mirrors 4d7a: a command pre-check that refuses to author the bad state, backed by the validate invariant as the unbypassable backstop. It beats A-as-is (no body signal) and B-as-is (presence-only via a brittle parser) on faithfulness, robustness, blast-radius, and precedent-fit at once.

SCOPE REFINEMENT (human-gated, confirmed at selection): the structured-frontmatter resolution adds a second (list_field) rule, a third (key-enum) handler, an integration_sections required field, and a one-integration-per-contract invariant - which EXCEEDS A's literal Non-scope ('no second validator rule of any kind beyond the one coverage handler'; 'section presence is NOT machine-checked'). As the human approval gate, this decision records the agreed refinement: machine-checking section presence via STRUCTURED FRONTMATTER (not body prose) is in-scope for the brief, because it makes the intent's headline value falsifiable using existing shape handlers while preserving A's bodies-are-convention doctrine and smallest-surface stance. Flagged, not absorbed; the brief implements it as an explicit recorded enlargement of A's validator surface.

GRAFTS FROM THE REJECTED CANDIDATE B (recorded, not absorbed): (1) the DISCIPLINE that body completeness must produce a machine signal, re-homed onto structured frontmatter; (2) B's SINGLE-SOURCE-OF-TRUTH requirement for the section vocabulary - satisfied by making the integration-reviewer agent the one canonical declaration, plus a test asserting the agent's documented section set is byte-equal to the configured key list so drift fails the suite not production; (3) B's DISPATCH-PINNING rigor (pin id/kind/handler-filename for every new rule; validator.ts hard-fails on an unknown kind); (4) B's example-8 PRESENCE-NOT-SUBSTANCE honesty bound, baked into the finding text and CLAUDE.md so green is never over-read as 'verified'; (5) the FAIL-CLOSED and TWO-RULES-INTERACT-DISTINCT-KIND test discipline (the latter also satisfies d4f2's distinguishability concern). Do NOT graft B's body-prose parser, its heading-match/fenced-code/whitespace semantics, its second prose-parsing gate, or the per-type body-validation doctrine break.

CORRECTIONS THE BRIEF MUST APPLY (fixes from the review; none change the intent's behaviour): (1) RELEASE-BLOCKING - add a dated coverage_coherence_from cutoff (NOTE the loader reads comparison_required_from as a NAMED field, so this needs a small loader.ts edit, not free reuse) PLUS a committed pre-merge audit of all 6 live selects-edged intents, and pin within-PR ordering (this PR's own integration node, touches edges, and capability-paths data land BEFORE the rule is appended). (2) d4f2 - confirmed zero incoming edges (only a by-type.yaml listing); drive open->addressed by an EXPLICIT decision-recorded manual edit with provenance, NOT the auto-rule (the selects-scoped rule structurally cannot reach it); record that its unresolved.yaml reporting is deliberately replaced by hard-fail distinct-kind Finding[]. (3) touches-source - author touches from per-lane EVIDENCE (edge_endpoint_types enforces source=evidence; every live touches sources from evidence), not the integration node, and bind sensitive-path touches to the owning capability. (4) extract a shared coverage-traversal helper for the three rules (comparison_required, class_market_quorum, new coverage); add a one-integration-per-contract invariant; prefer dedicated docs/tests capabilities over annexing CLAUDE.md/tests into capability-spec-tooling-1a2b; keep sensitive_paths at specs/schema/** only. (5) specify /integrate and /prepare-evidence as idempotent/re-runnable with a clean-tree commit-only-on-green abort posture (converge an existing draft, never author a second; re-entrant STOP-and-ask). (6) persist d4f2 subsumption provenance (this decision node) and the specs/{nodes,graph,indexes}/** intentionally-unowned authorization (a durable dated artifact). (7) add tests: lane-enum validity (enum_constraint does NOT cover a lane field today), draft-integration-status boundary, brief-set-bounded coverage, dispatch-pinning, self-dogfood red-then-green, two-rules-interact distinct-kind, the new structured-field checks, AND a stale-integration row (a lane brief superseded/reworked after a final integration is recorded is caught by the existing bidirectional covered<->addressed rule - test it; no new rule needed). (8) pin /integrate's closing report and single-brief no-op message, the partial-integration remediation path, the /prepare-evidence STOP-and-ask prompt content, and an authoring-time section checklist sourced from the canonical vocabulary.

The four acceptance lines L1-L4 are delivered identically by the shared coverage core regardless of the body axis. The honest bound is preserved and stated: green asserts the integration node exists, is wired to final evidence for every live brief, and DECLARES the required sections of a well-typed shape - NOT that the combined tests truly ran; the integration-reviewer agent remains load-bearing for substance and list/body correspondence. Intent stays open until a final integration node integrating final evidence for every live brief closes it via /integrate. This decision cites the market's comparison node comparison-lane-integration-8d3c (which holds the full critic-by-axis analysis); this decision holds the choice. This decision node is also the durable provenance record that subsumes intent-status-coherence-d4f2 (its single-lane coherence rule is landed and generalised by this work; d4f2 is driven open->addressed by an explicit recorded transition when this work's evidence lands, not by the auto-rule).
