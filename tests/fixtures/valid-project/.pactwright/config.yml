version: 1

# No version constraint: the fixture tracks whatever runtime it is built
# against, so a runtime bump does not have to touch every fixture.
agent_pack:
  source: "@pactwright/standard"

adapter:
  type: claude-code

extensions: {}

github:
  enabled: false
